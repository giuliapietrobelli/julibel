// Feature flags for development and temporary overrides.
// Set `HIDE_COOKIE_BANNER_GLOBALLY = true` to hide the cookie consent
// banner for all visitors. Change to `false` to restore normal behaviour.
export const HIDE_COOKIE_BANNER_GLOBALLY = true;
